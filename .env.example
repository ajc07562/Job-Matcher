# Optional — without this, the app still runs and ranks matches,
# it just skips the "why this fits" / gap-analysis text generation.
ANTHROPIC_API_KEY=

# Where the FastAPI backend listens
API_HOST=127.0.0.1
API_PORT=8000
